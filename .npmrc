# The app has an unavoidable peer-dep conflict (html-webpack-plugin expects webpack,
# while the bundler is @rspack/core@2). legacy-peer-deps lets npm install / npm ci
# resolve the tree anyway — required locally AND in CI (.github/workflows/deploy.yml
# runs plain `npm ci`, which honors this file).
legacy-peer-deps=true
